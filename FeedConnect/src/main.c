void app_main() {
    // Initialisations, configurations générales, etc.

    // Configurer le serveur Bluetooth
    setup_ble_server();

    // Boucle principale de l'application
    while (1) {
        // Votre logique principale ici
    }
}